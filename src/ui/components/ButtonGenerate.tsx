import React from "react";

export const ButtoGenerate = () => {

    const onClick = () => {
        //tbd
      };

    return (
        <button className="brand" onClick={onClick}>
          Generate
        </button>
    )
}